<% if (typeof errors != 'undefined') { %>
    <% for (error of errors) { %>
        <div class="alert alert-warning col-md-5 text-center mx-auto" role="alert">
            <%= error.msg %>
        </div>
    <% } %> 
<% } %> 